import React from 'react';
import {IconUser, LoginBlock} from "./style";

const Login = () => {
    return (
        <LoginBlock>
            <IconUser></IconUser>
        </LoginBlock>
    );
};

export default Login;